<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mihce">
    <!-- permiso dbConnect -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Consulta" />
        <activity android:name=".ConsultaVer" />
        <activity android:name=".FarmacoEditar" />
        <activity android:name=".Farmacos" />
        <activity android:name=".FarmacosForm" />
        <activity android:name=".Home" />
        <activity android:name=".MiHCE" />
        <activity android:name=".MisDatos" />
        <activity android:name=".MisDatosForm" />
        <activity android:name=".UserRegistration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>